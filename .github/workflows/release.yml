name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g. 1.2.3)"
        required: true
        type: string
      git_ref:
        description: "Git ref to release from"
        required: true
        default: "main"
        type: string

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  MIX_ENV: prod
  RELEASE_VERSION: ${{ inputs.version }}

jobs:
  release:
    name: Build Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.git_ref }}
          fetch-depth: 0

      - name: Setup BEAM
        uses: erlef/setup-beam@v1
        with:
          otp-version: "28"
          elixir-version: "1.18.4"

      - name: Restore Mix cache
        uses: actions/cache@v5
        with:
          path: |
            deps
            _build
            ~/.hex
            ~/.mix
            node_modules
            apps/manifold_web/node_modules
          key: ${{ runner.os }}-release-${{ hashFiles('mix.lock', 'package-lock.json', 'package.json', 'apps/manifold_web/package.json') }}
          restore-keys: |
            ${{ runner.os }}-release-
            ${{ runner.os }}-mix-

      - name: Configure git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Update Mix project versions
        run: |
          perl -0pi -e 's/version: "[^"]+"/version: "$ENV{RELEASE_VERSION}"/g' mix.exs apps/*/mix.exs

      - name: Fetch dependencies
        run: mix deps.get --only prod

      - name: Configure build-only runtime secrets
        run: |
          echo "SECRET_KEY_BASE=$(openssl rand -hex 64)" >> "$GITHUB_ENV"
          echo "MANIFOLD_CONNECTOR_ENCRYPTION_KEY=$(openssl rand -base64 32 | tr -d '\n')" >> "$GITHUB_ENV"

      - name: Install npm packages
        # WORKAROUND(upstream): duskmoon-dev/phoenix-duskmoon-ui#106
        run: mix run --no-start -e 'Application.ensure_all_started(:ssl); Mix.Task.run("npm.ci")'

      - name: Build assets
        run: mix assets.deploy

      - name: Build release artifact
        run: |
          mix release manifold --overwrite
          mix release manifold_edge --overwrite
          tar -C _build/prod/rel -czf manifold-${RELEASE_VERSION}.tar.gz manifold
          tar -C _build/prod/rel -czf manifold-edge-${RELEASE_VERSION}.tar.gz manifold_edge

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: manifold-${{ inputs.version }}
          path: |
            manifold-${{ inputs.version }}.tar.gz
            manifold-edge-${{ inputs.version }}.tar.gz
          if-no-files-found: error

      - name: Commit version bump
        run: |
          git add mix.exs apps/*/mix.exs mix.lock
          git commit -m "chore(release): v${RELEASE_VERSION}"

      - name: Tag release
        run: git tag "v${RELEASE_VERSION}"

      - name: Push commit and tag
        run: |
          git push origin HEAD
          git push origin "v${RELEASE_VERSION}"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "v${RELEASE_VERSION}" \
            --generate-notes \
            --title "v${RELEASE_VERSION}" \
            manifold-${RELEASE_VERSION}.tar.gz \
            manifold-edge-${RELEASE_VERSION}.tar.gz
